# ============================================
# Required Environment Variables
# Copy this file to .env and fill in values
# ============================================

# --- AI / LLM ---
# Anthropic API key, only needed if a downstream integration calls the
# Claude API directly outside of Claude Code itself.
ANTHROPIC_API_KEY=
